# Get allowed verbs for rules

Returns the list of valid verification verbs that can be used in
validation rules. Each verb corresponds to a different type of
validation check.

## Usage

``` r
get_allowed_rule_verbs()
```

## Value

Character vector of allowed rule verbs
