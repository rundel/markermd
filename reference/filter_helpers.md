# Helper Functions for Question Filters

Centralized helper functions that define allowed values for filter
conditions and build the q2r predicate expressions they map onto. These
functions serve as the single source of truth for filter validation.
