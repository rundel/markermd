# Add Question to Template

Add Question to Template

## Usage

``` r
add_question(tmpl, q)
```

## Arguments

- tmpl:

  template object

- q:

  question object

## Value

The `markermd_template` object with `q` appended to its questions.
