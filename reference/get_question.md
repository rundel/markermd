# Get Question by ID

Get Question by ID

## Usage

``` r
get_question(tmpl, id)
```

## Arguments

- tmpl:

  template object

- id:

  Integer question ID

## Value

The matching `markermd_question` object, or `NULL` when `id` is not
found.
