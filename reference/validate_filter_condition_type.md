# Validate filter condition type

Checks if a filter condition type is valid according to the allowed
types.

## Usage

``` r
validate_filter_condition_type(type)
```

## Arguments

- type:

  Character. The condition type to validate

## Value

Character error message if invalid, NULL if valid
