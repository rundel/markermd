% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{marks_import}
\alias{marks_import}
\title{Import grading marks from YAML into a project's database}
\usage{
marks_import(
  path,
  project = ".",
  overwrite = FALSE,
  repo = NULL,
  question = NULL
)
}
\arguments{
\item{path}{Path to a marks \code{.yaml}/\code{.yml} file. Absolute or relative to
the current directory; a bare relative path is also resolved against the
project root.}

\item{project}{Path to the project directory. Defaults to the working directory.}

\item{overwrite}{When \code{TRUE}, pairs with existing grading activity are
re-marked instead of skipped.}

\item{repo}{Optional character vector restricting the import to those
repository names within the file.}

\item{question}{Optional character vector restricting the import to those
question names within the file.}
}
\value{
Invisibly, a data frame with one row per (repository, question)
pair: columns \code{repo}, \code{question}, \code{action} (\code{"written"} or \code{"skipped"}),
and \code{n_selected}.
}
\description{
Reads a marks YAML file (see \code{\link[=marks_export]{marks_export()}} for the format) and records
the rubric item selections and comments it describes in the project's
grading database, as if a grader had toggled them in \code{\link[=mark]{mark()}}. Rubric items
are identified by their description text, matched verbatim against each
question's rubric.
}
\details{
Each (repository, question) entry is declarative: the listed items are
selected and every other rubric item of that question is explicitly
deselected, so \verb{items: []} records that no items apply. A pair imported
with no selected items and no public comment still shows as ungraded in
\code{\link[=mark]{mark()}} until a human confirms it, by design: machine-written marks are
suggestions awaiting review.

Pairs that already have any grading activity (any recorded selection event,
or a non-empty public or private comment) are skipped and reported unless
\code{overwrite = TRUE}, so an automated pass cannot silently clobber a human's
grading. An omitted \code{comment} / \code{private_comment} field leaves the stored
comment unchanged, while an empty string clears it.

All validation (repository names against the project's repos directory,
question names against the stored template, item descriptions against each
question's rubric) happens before anything is written, and the import
itself is a single transaction.
}
\seealso{
\code{\link[=marks_export]{marks_export()}}, \code{\link[=marks_set]{marks_set()}}, \code{\link[=read_marks_yaml]{read_marks_yaml()}},
\code{\link[=validate_marks_file]{validate_marks_file()}}, \code{\link[=rubric_import]{rubric_import()}}
}
