% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project-classes.R
\name{markermd_project}
\alias{markermd_project}
\title{Markermd Project}
\usage{
markermd_project(
  root = character(0),
  repos = NA_character_,
  comments = NA_character_,
  database = ".markermd/markermd.sqlite",
  key = NA_character_,
  artifacts = character(0),
  created_at = get_current_timestamp(),
  updated_at = get_current_timestamp(),
  version = markermd_project_version()
)
}
\arguments{
\item{root}{Character. Absolute, normalized project root (parent of \verb{.markermd/}).}

\item{repos}{Character. Root-relative repos directory, or \code{NA} if absent.}

\item{comments}{Character. Root-relative comments directory, or \code{NA} if absent.}

\item{database}{Character. Root-relative path to the SQLite grading database,
fixed at \code{.markermd/markermd.sqlite}. It is intrinsic to the project marker
and not user-configurable; the value is recorded for reference only. The
grading template is stored inside this database, not in the config.}

\item{key}{Character. Root-relative key (solution) repository directory, or \code{NA} if unset.}

\item{artifacts}{Character vector. Root-relative artifact directory names.}

\item{created_at}{Character. Project creation timestamp.}

\item{updated_at}{Character. Last config-update timestamp.}

\item{version}{Character. Config format version.}
}
\description{
S7 class describing an initialized markermd project. Path
properties are stored relative to \code{root} (resolved to absolute paths at
read time); \code{root} itself is the absolute project directory and is not
serialized to the config file. Timestamps are stored as strings (matching
the database layer's \code{get_current_timestamp()} format) so they round-trip
through YAML without timezone/format fragility.
}
