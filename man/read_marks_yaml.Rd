% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks-serialization.R
\name{read_marks_yaml}
\alias{read_marks_yaml}
\title{Read grading marks from a YAML file}
\usage{
read_marks_yaml(path)
}
\arguments{
\item{path}{Path to a marks \code{.yaml}/\code{.yml} file.}
}
\value{
A marks list: \code{format_version} plus \code{repos}, each with \code{name} and
\code{questions}, each question with \code{name}, \code{items} (a character vector of
rubric item descriptions) and \code{comment} / \code{private_comment} (a string, or
\code{NULL} when the file omits it).
}
\description{
Parses a marks YAML file into the marks list structure used by
\code{\link[=marks_import]{marks_import()}} and \code{\link[=write_marks_yaml]{write_marks_yaml()}}. Rubric items are identified by
their description text, which must match the question's rubric verbatim;
the match itself is performed (and validated) by \code{\link[=marks_import]{marks_import()}}, not
here.
}
\seealso{
\code{\link[=write_marks_yaml]{write_marks_yaml()}}, \code{\link[=marks_import]{marks_import()}}, \code{\link[=validate_marks_file]{validate_marks_file()}}
}
