% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rubric-serialization.R
\name{read_rubric_yaml}
\alias{read_rubric_yaml}
\title{Read a grading rubric from a YAML file}
\usage{
read_rubric_yaml(path)
}
\arguments{
\item{path}{Path to a rubric \code{.yaml}/\code{.yml} file.}
}
\value{
A rubric list: \code{format_version} plus \code{questions}, each with \code{name},
\code{items} (a list of \code{markermd_rubric_item} objects) and \code{scoring} (a
\code{markermd_grade_state} object, or \code{NULL} when the file omits it).
}
\description{
Parses a rubric YAML file into the rubric list structure used by
\code{\link[=rubric_import]{rubric_import()}} and \code{\link[=write_rubric_yaml]{write_rubric_yaml()}}. Item order in the file
determines display order and keyboard hotkeys (1-10 by position, none
beyond ten). Validation is performed by the S7 constructors as items are
built.
}
\seealso{
\code{\link[=write_rubric_yaml]{write_rubric_yaml()}}, \code{\link[=rubric_import]{rubric_import()}}, \code{\link[=validate_rubric_file]{validate_rubric_file()}}
}
