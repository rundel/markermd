% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{init_project}
\alias{init_project}
\title{Initialize a markermd grading project}
\usage{
init_project(path)
}
\arguments{
\item{path}{Path to the project directory.}
}
\value{
A \code{markermd_project} object, invisibly.
}
\description{
Bootstraps a markermd project in a directory laid out by
\code{ghclass::org_grade_assignment()} (a \verb{repos/} directory of student
repositories, a \verb{comments/} directory, an optional key/solution repository,
and a directory per downloaded artifact). It creates a \verb{.markermd/} directory
at the project root holding the SQLite grading database and a \code{config.yml}
recording the project's file locations, and installs the bundled Claude Code
skills into \verb{<path>/.claude/skills/} so they are discoverable from the
project.
}
\details{
The key (solution) repository is detected as the top-level git repository
(student repositories live under \verb{repos/}); when several top-level repos
exist, the one whose name contains "key" is used. When no top-level git
repository exists (a copied or unpacked layout, such as the bundled example
project), a directory whose name contains "key" and that holds an assignment
document (\code{.qmd}/\code{.Rmd}) is used instead. Remaining top-level directories
that are not git repositories are recorded as artifact directories.

Re-running \code{init_project()} on an already-initialized directory updates it in
place: the existing grading database (including any stored grading template)
is preserved, the skills are refreshed to the packaged version, the key and
artifact directories are re-scanned, and a previously configured key is kept
(or cleared if it no longer exists).
}
\examples{
\dontrun{
ghclass::org_grade_assignment("hw01", org = "my-course", repo_filter = "hw01-")
init_project("hw01")
}
}
