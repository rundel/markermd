% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{rubric_import}
\alias{rubric_import}
\title{Import a grading rubric from YAML into a project's database}
\usage{
rubric_import(
  path,
  project = ".",
  mode = c("append", "replace"),
  question = NULL
)
}
\arguments{
\item{path}{Path to a rubric \code{.yaml}/\code{.yml} file. Absolute or relative to
the current directory; a bare relative path is also resolved against the
project root.}

\item{project}{Path to the project directory. Defaults to the working directory.}

\item{mode}{Either \code{"append"} or \code{"replace"}; see Details.}

\item{question}{Optional character vector restricting the import to those
question names within the file.}
}
\value{
The parsed rubric list, invisibly.
}
\description{
Reads a rubric YAML file (see \code{\link[=rubric_export]{rubric_export()}} for the format) and applies
it to the project's grading database. Item order in the file determines
display order and keyboard hotkeys. This is the inverse of \code{\link[=rubric_export]{rubric_export()}}.
}
\details{
With \code{mode = "append"} (the default) the file's items are added after each
question's existing items. With \code{mode = "replace"} each affected question's
existing items are deleted first; this also deletes any recorded selections
of those items, for every repository, and cannot be undone. A question's
scoring setup is only updated when the file provides a \code{scoring} block.

Question names in the file must exactly match question names in the
project's stored template; mismatches abort before anything is written.
}
\examples{
\dontrun{
rubric_import("hw01-rubric.yaml", project = "hw01")

# Replace each affected question's existing items
rubric_import("hw01-rubric.yaml", project = "hw01", mode = "replace")
}
}
\seealso{
\code{\link[=rubric_export]{rubric_export()}}, \code{\link[=read_rubric_yaml]{read_rubric_yaml()}}, \code{\link[=validate_rubric_file]{validate_rubric_file()}}
}
