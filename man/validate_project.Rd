% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{validate_project}
\alias{validate_project}
\title{Validate a project's student repositories against its template}
\usage{
validate_project(path = ".", template = NULL, use_qmd = TRUE)
}
\arguments{
\item{path}{Path to the project directory. Defaults to the working directory.}

\item{template}{Optional path to a template \code{.yaml} overriding the one recorded
in the project config.}

\item{use_qmd}{Logical. Match \code{.qmd} files (TRUE) or \code{.Rmd} files (FALSE).}
}
\value{
A data frame with one row per repository/question: columns \code{repo},
\code{question}, \code{status} (\code{"pass"}, \code{"fail"}, or \code{"error"}), and \code{detail}. A repo
whose assignment cannot be found or parsed yields a single \code{"error"} row.
}
\description{
Loads the project's grading template and validates every student repository
(discovered from the project config's \code{repos} directory) against the
template's questions and rules. This is the same section-based matching used
during grading, run headlessly so a freshly scaffolded template can be
sanity-checked: a rule that fails across most repositories is usually too
strict and should be relaxed in \code{template()}.
}
\details{
Each repository's assignment file is matched by the basename recorded in the
template's \code{source.path} (falling back to the first matching document), so the
template's heading/div anchors line up with the student documents.
}
