% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{marks_set}
\alias{marks_set}
\title{Record grading marks for one repository/question pair}
\usage{
marks_set(
  repo,
  question,
  items = NULL,
  comment = NULL,
  private_comment = NULL,
  project = ".",
  overwrite = FALSE
)
}
\arguments{
\item{repo}{Repository name (a directory under the project's repos directory).}

\item{question}{Question name from the project's template.}

\item{items}{Character vector of rubric item descriptions to select
(\code{character(0)} to deselect everything), or \code{NULL} to leave selections
unchanged.}

\item{comment}{Public, student-facing comment; an empty string clears the
stored comment, \code{NULL} leaves it unchanged.}

\item{private_comment}{Private grader note, never shown to students; an
empty string clears it, \code{NULL} leaves it unchanged.}

\item{project}{Path to the project directory. Defaults to the working directory.}

\item{overwrite}{When \code{TRUE}, a pair with existing grading activity is
re-marked instead of raising an error.}
}
\value{
Invisibly, a one-row data frame with columns \code{repo}, \code{question},
\code{action} (\code{"written"}), and \code{n_selected} (\code{NA} when \code{items} is \code{NULL}).
}
\description{
Programmatically marks a single (repository, question) pair in the
project's grading database, without going through a marks YAML file: the
one-off counterpart to \code{\link[=marks_import]{marks_import()}}. Rubric items are identified by
their description text, matched verbatim.
}
\details{
\code{items} is declarative: the listed items are selected and every other
rubric item of the question is deselected, so \code{character(0)} records that
no items apply. \code{items = NULL} leaves the pair's selections untouched and
only writes the supplied comments. At least one of \code{items}, \code{comment}, or
\code{private_comment} must be supplied.

Unlike \code{\link[=marks_import]{marks_import()}}, which skips pairs that already have grading
activity, this targeted setter errors on such a pair unless
\code{overwrite = TRUE}, so a script cannot believe a write happened when it was
ignored.
}
\seealso{
\code{\link[=marks_import]{marks_import()}}, \code{\link[=marks_export]{marks_export()}}
}
