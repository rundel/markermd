% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{rubric_export}
\alias{rubric_export}
\title{Export a project's grading rubric to YAML}
\usage{
rubric_export(path, project = ".", question = NULL)
}
\arguments{
\item{path}{Output path for the rubric \code{.yaml} file.}

\item{project}{Path to the project directory. Defaults to the working directory.}

\item{question}{Optional character vector of question names to export.
Defaults to every question in the project's template.}
}
\value{
The output \code{path}, invisibly.
}
\description{
Writes the rubric stored in the project's grading database (each question's
rubric items, in display order, plus its scoring setup when one has been
configured) to a YAML file. The file contains no per-repository grading
data, so it can be shared, edited by hand or by an LLM tool, and brought
back with \code{\link[=rubric_import]{rubric_import()}}. The file format is described by the JSON
Schema at \code{system.file("schema/markermd-rubric.json", package = "markermd")}.
}
\seealso{
\code{\link[=rubric_import]{rubric_import()}}, \code{\link[=template_export]{template_export()}}
}
