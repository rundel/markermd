% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{export_comments}
\alias{export_comments}
\title{Export student-facing feedback to per-repository markdown files}
\usage{
export_comments(project = ".")
}
\arguments{
\item{project}{Path to the project directory. Defaults to the working directory.}
}
\value{
The paths of the written markdown files, invisibly.
}
\description{
Writes each student repository's public feedback to
\verb{<comments>/<repo>.md} under the project root, using the project's
configured comments directory (\verb{comments/} when none is configured; it is
created when missing). Each graded question appears as a heading, in
template order, followed by a bulleted markdown list of its selected
rubric item descriptions and its public comment. Private comments are
never exported, and a repository with no public feedback gets no file.
}
\examples{
\dontrun{
export_comments("hw01")
}
}
\seealso{
\code{\link[=export_scores]{export_scores()}}, \code{\link[=export_marks]{export_marks()}}, \code{\link[=marks_export]{marks_export()}}
}
