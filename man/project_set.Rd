% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_set}
\alias{project_set}
\title{Change a markermd project's configuration}
\usage{
project_set(
  path = ".",
  repos = NULL,
  comments = NULL,
  template = NULL,
  key = NULL
)
}
\arguments{
\item{path}{Path to the project directory. Defaults to the working directory.}

\item{repos}{Root-relative repos directory to record. Warns if it does not exist.}

\item{comments}{Root-relative comments directory to record. Warns if it does not exist.}

\item{template}{Root-relative path to a grading template to record (the file
must exist), or \code{NA} to clear a previously configured template.}

\item{key}{Root-relative key (solution) repository directory to record. Warns
if it does not exist. Pass \code{NA} to clear a previously configured key.}
}
\value{
The updated \code{markermd_project} object, invisibly.
}
\description{
Updates path entries in a project's \code{config.yml}. Only supplied arguments are
changed; an omitted (\code{NULL}) argument is left untouched. Paths are interpreted
relative to the project root.
}
